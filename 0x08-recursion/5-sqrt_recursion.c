#include "main.h"

/**
 * _sqrt_recursion - find square
 * @n: given number
 * Return: square
 */

int _sqrt_recursion(int n)

{
	int i;

	if (n < 0 || n == 1)
		return (-1);

	if (i < n || i * i == n)

	return (n * _sqrt_recursion(n + 1));


}
